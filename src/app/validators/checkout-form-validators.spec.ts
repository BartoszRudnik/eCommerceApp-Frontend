import { CheckoutFormValidators } from './checkout-form-validators';

describe('ShopValidators', () => {
  it('should create an instance', () => {
    expect(new CheckoutFormValidators()).toBeTruthy();
  });
});
